# Local .terraform directories
**/.terraform/*

# .tfstate files (IMPORTANT: Exclude these, especially if they contain secrets)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
# (e.g. passwords, private keys, API tokens)
*.tfvars
*.tfvars.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore plan files
*.tfplan
